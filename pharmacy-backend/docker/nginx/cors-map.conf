# يُحمَّل من http { include conf.d/*.conf } — يجب أن يكون قبل server blocks
map $http_origin $cors_allow_origin {
    default "*";
    "~^https://[a-zA-Z0-9.-]+\.vercel\.app$" $http_origin;
    "~^http://localhost(:[0-9]+)?$" $http_origin;
}
